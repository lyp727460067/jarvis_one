cmake_minimum_required(VERSION 3.5)
project(jarvis_top_pic)
if(PLATFORM_mr527)
# include(${TOP_CMAKE_DIR}/vslam/jarvis_pic)
add_subdirectory(${TOP_CMAKE_DIR}/vslam/jarvis)
add_subdirectory(${TOP_CMAKE_DIR}/vslam/jarvis_pic)
endif()
